#include<stdio.h>
int main(){
    float m1,m2,m3,m4,m5,sum,percentage;
    printf("enter the marks of the 5 subjects to find its sum and percentage out of 100:\n");
    scanf(" %f%f%f%f%f",&m1,&m2,&m3,&m4,&m5);
    sum= (m1+m2+m3+m4+m5);
    percentage=(sum/500)*100;
    printf("the sum of the 5 subjects marks is %f and the percentage is %f",sum,percentage);
    return 0;
    
}
