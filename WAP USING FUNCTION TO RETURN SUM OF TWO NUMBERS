#include<stdio.h>

int sum(int a,int b); 

int main(){
    int a,b;
    printf("enter two numbers a and b to find its sum:\n");
    scanf("%d%d",&a,&b);
    printf("the sum of %d and %d : %d",a,b,sum(a,b));
    return 0;
}

int sum(int c,int d){
    return c+d;
}
