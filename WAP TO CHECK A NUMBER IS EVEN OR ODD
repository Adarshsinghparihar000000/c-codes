#include<stdio.h>
int main(){
    int a;
    printf("enter number a to check it is even or odd:");
    scanf("%d",&a);
    if(a%2==0){
        printf("%d is even",a);
    }
    else if(a%2 != 0){
    printf("%d is odd",a);
    }
    else printf("enter a valid number");
    return 0;
}
